apiVersion: apps/v1
kind: Deployment
metadata:
  name: net
  namespace: net-pre
  labels:
    app: net
spec:
  replicas: 1
  selector:
    matchLabels:
      app: net
  template:
    metadata:
      labels:
        app: net
    spec:
      imagePullSecrets:
      - name: my-docker-credentials
      containers:
      - name: net
        image: udeccolima/net:v1pre  
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        env:
        - name: ENDPOINT
          value: mysql-net 
        - name: DATABASE
          value: proyecto 
        - name: USERD
          value: user 
        - name: PASSD
          valueFrom:
            secretKeyRef:
              name: deploy-secret 
              key: deploy-password 
---
apiVersion: v1
kind: Service
metadata:
  name: net
  namespace: net-pre
spec:
  selector:
    app: net
  ports:
    - protocol: TCP
      port: 80  
      targetPort: 8080 
  type: ClusterIP